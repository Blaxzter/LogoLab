import { DeviceMock } from './DeviceMock'

/** Real iPhone home-screen screenshot with the logo composited as an app icon. */
export default function PhoneHomeIOS() {
  return <DeviceMock id="ios" />
}

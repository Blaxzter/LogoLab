import { DeviceMock } from './DeviceMock'

/** Real Android home-screen screenshot with the logo composited as an app icon. */
export default function PhoneHomeAndroid() {
  return <DeviceMock id="android" />
}
